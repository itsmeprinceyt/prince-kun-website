# Steps

1. Open `https://prince-timestamp-generator.vercel.app/` and copy the timestamp
2. Make a folder inside `./Migrations/{timestamps}_file_name.sql`
3. Restart the server.

> The migration file would be applied automatically.
